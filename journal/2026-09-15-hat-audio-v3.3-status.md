# 2026-09-15 — HAT audio (hat-sound v3.3) status handoff

Governing plan: `plans/current/2026-09-15-10-01-56_hat-sound-setup-fold-in.md`.
This note is a complete handoff so any future agent can pick up the
**open pacing anomaly** and the **setup.sh fold-in** without re-deriving
what happened here.

## Where things stand (one paragraph)

Real-audio mode is **done in source**: `tools/hat-sound.c` plays raw
s16le 48 kHz mono through the proven 192 kHz / OS=4 sigma-delta tick
clock, feeds via a ring, drains at EOF, parks the amp pin LOW, and exits
0. **The 4× pacing bug is ROOT-CAUSED and fixed (v3.4)**: the 4-tick
overrun re-anchor at the bottom of `main()`'s loop compared
`now - next` UNSIGNED; on a healthy clock `now < next`, so the
difference underflows to ~2^64 and the test was true EVERY tick —
resetting `tis` to 0 every tick. Mode 0 then advanced its sine phase
every tick (all sigma tones ~4× pitch — the long-unexplained "modem"
character of early runs) and mode 2 consumed a ring sample EVERY tick
(4× real-time; the self-report run measured **3.98×**). A bench that
links the real engine looked correct because it never ran `main()` —
the engine alone was always 1 sample / 4 ticks; only the loop around it
was broken. `-p` mode was immune (elapsed-time math only), which is why
the square wave always sounded right. Fix: signed compare
`(int64_t)(now - next) > (int64_t)(4*TICK_NS)`. The v3.4 binary is
built (10:14, md5 `38281b372cb30c292a02efb3e0e59a5f`). **The gate ran
and PASSED (operator run, 2026-09-15): self-report `t=5.5s
ticks=1055990 … x10ms=0 und=1` — 1 055 990 = 5.5 s × 192 kHz exactly
(v3.3 was ~264 k), 4 tones at normal pitch, operator ear-clean. AUDIO
IS DONE, fix committed `a2b7dc8`.** v3.5 added `-i` idle mode (operator
choose the silent/low-power pin-LOW boot behavior); the setup.sh
fold-in is WRITTEN in the tree (binary build, `gamepi-sound.service`,
sysctl drop-in, `user_overlays` set-convergence, stale
es8388/i2c0 removal, docs rows — same change) and awaits one live
`apply -> verify` run before commit.

## What "done" looks like (definition of done)

Real audio is DONE when a on-board `sudo /home/cj/hat-sound -F
/home/cj/test.s16` run prints, on its final self-report line,
`... consumed 528000 B (264000 samples, 48000.0 Hz, 1.00x real-time)
, underruns ~0` AND the operator hears the 4 tones at normal pitch
(440 Hz, rising chirp, 1 kHz, quiet 440 Hz tail), total ~5.5 s.
**MET 2026-09-15:** operator run self-reported `ticks=1055990` (5.5 s ×
192 kHz), 4 tones at normal pitch, ear-clean — gate closed. The
fold-in (next sections) is written in the tree.

## The exact state of the tree

- Repo: branch `main`, ~7 ahead of `origin` (`346404a`); HEAD
  `a2b7dc8` = v3.4 signed-compare fix + journal + plan. **TREE DIRTY
  with the fold-in**, awaiting one live `apply -> verify` commit:
  `tools/hat-sound.c` (v3.5), `setup.sh`, `docs/setup.md`, this
  journal, the plan.
- `tools/hat-sound.c` is **tracked since `a63a759`** (first git
  baseline); v3.4 committed at `a2b7dc8`; the v3.5 delta (uncommitted)
  = `-i` idle mode + header/run-line/usage + version strings.
- `tools/hat-sound.c` = v3.5 source, LATEST on disk. Contains (in this
  order, with approximate line numbers):
  - L157-163 audio grid: `TICK_HZ 192000.0`, `OS 4u`, `AUD_HZ = TICK_HZ/OS`
    (48000), `SD_FS 8192`, `GAIN 0.85` (program mode only),
    `TICK_NS (uint64_t)(1e9/TICK_HZ)` (5208).
  - L170-189 ring: `RING_MAX 8192u` (bytes), `g_mask = RING_MAX-1`;
    `_Atomic unsigned long g_prod, g_cons` (monotonic byte counters,
    masked on use); `_Atomic int g_feed_eof`; `_Atomic uint64_t
    g_underrun`; `int g_src_fd` (-1 unused / 0 stdin / >0 file);
    `char g_src_name[256]`. 2 tail bytes reserved so full≠empty.
    `ring_free() = (RING_MAX-2) - (g_prod-g_cons)`, clamped ≥ 0.
  - L201 `feed_more()`: `free_n=ring_free(); if 0 return -10;`
    `at = g_prod & g_mask; tail = RING_MAX-at; if (free_n>tail) free_n=
    tail;` (caps each read at the buffer tail — never straddles the end,
    THE segfault fix); `read(g_src_fd, g_ring+at, free_n)`; r>0 →
    `g_prod+=r, return 0`; EINTR retry; EAGAIN usleep(2000) retry; r<0 →
    `fprintf(stderr,"hat-sound: read source: %s\n",strerror(errno));
    return -1`; r==0 → `fprintf(stderr,"hat-sound: EOF after %lu source
    bytes\n",(unsigned long)g_prod); return -1`.
  - L222 `feed_thread()` (stdin/`-F -` only): r==0 continue; r==-10 →
    usleep(10000) continue (ring full, NOT EOF); else break →
    `g_feed_eof=1`.
  - L235 `sample_next(int16_t *s16)`: `if (g_cons + 2 <= g_prod)` →
    `p = g_cons & g_mask; hi = g_ring[(p+1)&g_mask]; *s16 = g_ring[p] |
    (hi<<8); g_cons += 2; return 1;` else `g_underrun++; return 0`
    (hold last sample).
  - L252 `fd_head_check()`: 16-byte peek for `ID3`/`RIFF…WAVE`; prints
    "wants RAW s16le 48 kHz mono" + re-encode recipe; lseek back to 0.
  - L418 `engine_tick(...)` mode 2 (THE pacing path, current on disk):
    ```c
    if (mode == 2) {
        if (*tis == 0)
            (void)sample_next(s16);
        *tis = (*tis + 1u) & (OS - 1u);
        return sd_bit((int16_t)(((int32_t)*s16 * (int32_t)SD_FS) >> 15));
    }
    ```
    s16 full-scale (±32768) → ±SD_FS via `>> 15` (÷4), so a loud file
    can't overflow the quantizer into hard clipping.
  - L380 `sd_bit()` (1st-order 1-bit ΣΔ):
    `int o=(g_sda>0); g_sda += (int64_t)x - (int64_t)(o?SD_FS:-SD_FS);
    return o;`
  - L391 `wait_deadline()`: **pure spin**
    `while ((uint64_t)(*now = now_ns()) < next) {}` (frozen, do NOT
    retune).
  - The 4-tick overrun re-anchor (bottom of the loop, v3.4):
    `if ((int64_t)(now - next) > (int64_t)(4 * TICK_NS)) { next = now
    + TICK_NS; tis = 0; }` — **signed compare; the v3.3 unsigned
    `now - next` underflowed on every healthy tick, forced `tis=0`
    every tick, and caused the 4× rate. Do not "simplify" it back.**
  - L479 `main()` (v3.4 layout; v3.5 main is ~+18 lines longer): CLI
    `-F FILE|-` sets raw_src (`-` = stdin, else `g_src_name`); `-i`
    sets idle (v3.5); `-F` + `-p`/`-t` → usage error rc 2 (as does
    `-i` + any source/tone arg); raw_src ⇒ mode=2. v3.5 idle branch
    (after `pin_acquire()`, before feeder/`rt_setup()`): banner, then
    `while (!g_stop) pause();` (signal handlers installed earlier),
    `pin_release()` (LOW), exit 0 — no feeder thread, no RT setup;
    this is the boot-service mode.
    Ring `malloc`. File → `open(O_RDONLY|O_CLOEXEC)` + `fd_head_check()`.
    stdin → isatty warning. `pin_acquire()` (GPIO v2, AMP_LINE=37,
    PB5/header pin 12). Feeder pthread ONLY when `g_src_fd==0`, created
    BEFORE `rt_setup()` (inherits CFS), pinned to neighbor core
    `(g_core % nc + 1) % nc` (nc from sysconf, min 2), named "hatfeed",
    NOT joined. `rt_setup()` = mlockall + PR_SET_TIMERSLACK 1000 +
    SCHED_FIFO prio 98 + affinity to g_core (default 1).
    Engine loop L637-681: seg-rollover stats;
    `if (mode==2 && g_feed_eof && g_cons+2 > g_prod) break;` (EOF, ring
    drained); `engine_tick(...)`; `pin_set(lvl)`; feed gate
    `if (mode==2 && g_src_fd>0 && !g_feed_eof && ring_free() >=
    RING_MAX/4) { if (feed_more()==-1) g_feed_eof=1; }` (the `!g_feed_eof`
    = the post-EOF spam fix); `wait_deadline(next,&now)`; dev/late/x1ms/
    x10ms stats; `next += TICK_NS`; 4-tick overrun re-anchor
    (`next = now + TICK_NS; tis = 0;`); `if (el >= total_ns) break;`
    (mode 2 `total_ns = UINT64_MAX`).
  - L684-699 (THE JUST-ADDED SELF-REPORT): after final `stats_line`,
    mode 2 prints to **stderr** (so it cannot be a stale stats-file
    read): `hat-sound: <stopped (signal)|EOF, ring drained> — source
    %lu B, consumed %lu B (%lu samples, %.1f Hz, %.2fx real-time),
    underruns %lu` where pps = consumed_samples / elapsed_s and the ×
    is pps / AUD_HZ. **This is the instrument that resolves the pacing
    question definitively on the next run.**
- `/home/cj/hat-sound` = **v3.5** build 12:19 (73 560 B, md5
  `bb1a9ad35b1ce61934cd067e600ef78d`), zero warnings = committed v3.4
  (`a2b7dc8`) + `-i` idle mode. Prior builds: v3.4 10:14 (73 520 B,
  md5 `38281b372cb30c292a02efb3e0e59a5f`); v3.3 01:52 (md5
  `0cc58c4bc4cc7261be944b52348e747f` — md5-verified identical to a
  fresh rebuild of the committed source, so the 4× was a SOURCE bug,
  not a stale binary). **Always rebuild + timestamp-check + md5 before
  handing the user a run** (stale binaries confused earlier windows).
- `/home/cj/hat-sound-fresh` = throwaway v3.3 md5-verification rebuild
  (gate now closed — DELETABLE: `rm /home/cj/hat-sound-fresh`).
- `/home/cj/test.s16` = 528 000 B (5.5 s s16le 48 kHz mono), python3
  stdlib generated: 440 Hz 1.0 s (amp 0.6) + 200→4000 Hz chirp 1.0 s
  (0.5) + 1000 Hz 1.0 s (0.6) + 440 Hz 2.5 s (0.25). Confirmed 48k from
  the transcript generator.
- `/home/cj/sd-bench.c` = standalone bench (compiles `hat-sound.c` into a
  `main` that calls `engine_tick` mode 2 for N ticks with a fake
  `g_prod` and reports `g_cons`). Output:
  `ticks=1057620 consumed=528810 bytes = 264405 samples` / `expect
  correct = 528810 bytes (t/2); 4x-bug would be 1057620 (t)` → **exit 0,
  consumed = t/2 = CORRECT 1-sample/4-ticks.** Build: `gcc -O2 -Wall
  -Wextra -o sd-bench sd-bench.c -lm -lpthread`. **Keep it** — it is the
  proof that the source is right.
- `/home/cj/hat-sound-v2.c` = keep (carry-forward).

## The debugging saga (what I tried / what worked / what didn't)

### Bug v1 — segfault, exit 139 (RUN 1, 01:42 binary)
`feed_more()` passed a `free_n` of up to 8190 bytes to `read()` at the
wrapped offset `g_prod & g_mask`; when `at` was near 8190 the write ran
past the 8 KB `malloc` (heap smash) ~10 ms in.
**Fix:** cap each read at `tail = RING_MAX - at` so a full ring fills
across two reads and nothing ever straddles the buffer end. Collateral:
odd-`read()`-byte tolerance in `sample_next` (`g_cons + 2 <= g_prod`) +
EOF-drain guard (`g_cons + 2 > g_prod`); `feed_thread` no longer treats
`-10` (ring full) as EOF (stdin pipe truncation). Rebuilt 01:44,
(RUN 2) exit 0.

### Bug v2 — post-EOF feed spam (RUN 3, 01:45 binary)
After EOF the engine feed gate kept calling `feed_more()` every tick
during ring drain → re-hit EOF, re-print `EOF after 528000 source bytes`
350×, and `fprintf` stole CPU from the 5.2 µs tick → sputtered tail.
**Fix:** `!g_feed_eof` guard on the gate. Applied to source (the run that
used it printed a clean single EOF line → **this fix is confirmed
working**).

### Bug v3 — the 4× pacing / 1.4 s anomaly — ROOT-CAUSED, FIXED v3.4
SYMPTOM: stats show `t=1.4s ticks≈264000-265000` for a 5.5 s / 1 056 000-
tick file, `und≈1`, `duty≈49-50%`, `x1ms=0 x10ms=0` (clean clock). The
whole 528 000-byte file IS consumed (self-report confirmed `consumed
528000 B (264000 samples, 191051.2 Hz, 3.98x real-time)`, operator's
screen, 10:10 run); it just happens in ~264 000 ticks instead of
~1 056 000. **Interpretation:** 264 000 samples / 264 000 ticks ≈ 1
sample per tick = 4× the intended 48 kHz ⇒ audio played ~4× fast (440
Hz heard as ~1760 Hz). The clock is NOT the cause (dev is tens of ns,
x10ms=0) — confirmed: 1.38 s × 192 kHz ≈ 264k ticks exactly.

**ROOT CAUSE (found by reading `main()`'s loop, which the bench never
exercised):** the 4-tick overrun re-anchor
```
if (now - next > 4 * TICK_NS) { next = now + TICK_NS; tis = 0; }
```
uses `uint64_t` operands. After `wait_deadline` on a HEALTHY clock
`now < next` (deadline still ahead) ⇒ `now - next` wraps to ~2^64 ⇒
comparison true EVERY tick ⇒ `tis` forced to 0 every tick. Since the
element's pacing key is `if (*tis == 0) sample_next()` (and mode 0's
phase advance), consumption/phase ran at one-per-tick instead of
one-per-4-ticks. It is invisible to: the engine bench (engine called
directly, no loop), the clock stats (re-anchoring a clean clock adds no
deviation — that's why x10ms=0 stayed clean), and `-p` (uses elapsed
time only). Also retro-explains: all sigma-delta tones in v3 runs being
~4× pitch ("modem"), and the "byte-identical stats across runs" being
DETERMINISTIC re-execution of the bug, not file staleness (that lesson
holds, but the real explanation was this).

**FIX (v3.4):** `if ((int64_t)(now - next) > (int64_t)(4 * TICK_NS))` —
signed; true condition now means genuinely behind by >4 ticks.
Documented in the code comment + file header. Rebuilt 10:14, zero
warnings, md5 `38281b372cb30c292a02efb3e0e59a5f` (73 520 B). Bench
re-run: still `consumed = t/2`, exit 0 (engine untouched). **CLOSED by
operator live run (2026-09-15):** self-report `t=5.5s
consumed=528000 B (264000 samples, 48000.0 Hz, 1.00x real-time),
ticks=1055990, dev_avg=54.7ns, dev_max=565019ns, late1us=2396
x1ms=0 x10ms=0, und=1, duty=50%` — 1 055 990 = 5.5 s × 192 kHz (v3.3
was ~264 k), 4 tones at normal pitch, operator ear-clean. Fix
committed `a2b7dc8`.**

WHAT I TRIED (in order):
1. Re-read stats + `read_file` the ring/engine/main — all consistent with
   4 ticks/sample. No smoking gun in the source.
2. Confirmed `/home/cj/test.s16` is 48 kHz from the transcript generator
   (not a 96 kHz or double-sample-rate file), size 528 000 = 5.5 s.
   **Rule out wrong-rate input file: RULED OUT.**
3. Wrote `sd-bench.c` to compile the EXACT `engine_tick` at -O2 and run
   1 057 620 ticks with a fake `g_prod`. Result: `g_cons = 528 810 = t/2`
   → **exactly 1 sample per 4 ticks. The source is PROVEN correct.**
4. Disassembled the real TU (`gcc -O2 -S`): `engine_tick` is INLINED
   into `main` (no standalone symbol); only 4 `imul 8192` sites
   (440Hz-sine gen, `>>15` ×3, etc.), no obvious duplicate ring
   consumer. Static asm read is inconclusive for a subtle inlined loop.
5. Added the self-report stderr line so the next on-board run self-rates
   instead of relying on a possibly-stale root-owned stats file I read
   across tool turns. **This is the decisive instrument; the run it
   rides on has not happened yet.**

LEARNED (the big lesson, do not skip):
- A stats file I `cat` can legitimately be **stale relative to the run I
  think I'm reading**. Two different runs returned byte-identical
  `t=1.4s ticks=2644xx` — the tell that a stale/read-timing race was
  confounding the signal. **Ground-truth must come from the operator's
  screen (the self-report line), not from a file I read.**
- Do NOT keep re-reading the same `/run/...` file expecting it to
  change between turns; pair every "the run happened" with a fresh
  `stat -c %y` and treat the timestamp as the authority for which run the
  content belongs to.
- A **bench that compiles the real engine source** is the fastest way to
  separate "source is wrong" from "field/environment is wrong." Prefer it
  before more on-board runs when a timing/counting mismatch resists
  source inspection.
- The tick path and clock are FROZEN (proven prior window); the anomaly is
  in **sample consumption rate**, not tick rate. Do NOT retune timing.

WHAT I WAS GOING TO TRY NEXT → RESOLVED (2026-09-15 ~10:15):
1. Done: operator ran the 01:52 self-report binary; the screen printed
   `... consumed 528000 B (264000 samples, 191051.2 Hz, 3.98x real-
   time), underruns 1` → the field IS 4× (not a stale-read artifact after
   all — the deterministic bug produced the same numbers every run, which
   is what had looked like staleness).
2. Done: re-read the loop (the bench-proven engine had excluded only the
   engine, not `main()`'s loop) → found the unsigned-`now - next`
   underflow in the re-anchor → fixed (signed compare) → v3.4 built
   10:14, bench still green (engine untouched), md5
   `38281b372cb30c292a02efb3e0e59a5f`.
3. DONE (2026-09-15): operator ran v3.4 → `1.00x real-time`,
   `ticks=1055990`, ear-clean → gate closed; fix committed `a2b7dc8`;
   session memory updated (bug v3 root cause + final numbers + gate
   closed).

## The setup.sh fold-in (WRITTEN in tree 2026-09-15 ~12:15, awaiting the
live apply→verify)

Contract anchors (as designed, verified this window):
- DESIRED block: `SOUND_BIN="/usr/local/bin/hat-sound"`,
  `SOUND_SRC="tools/hat-sound.c"` (caller-CWD-relative — document as a
  preflight), `SYSCTL_RT_DROPIN="/etc/sysctl.d/99-sched-rt.conf"`.
- `write_if_changed TARGET MODE OWNER CONTENT` (compare-then-write;
  identical bytes never touch disk; plan mode writes nothing).
- `apply_overlay()`: **set-convergence** — new helpers
  `overlay_desired_set()` / `overlay_uvs()` (LC_ALL=C-sorted, space-
  joined) make `user_overlays` drift a set comparison; a no-op on a
  healthy board; gpio-keys joins by extending ONE list later. Whole-
  line `sed` only when the set differs.
- `generate_unit()`: new `sound)` case mirroring the plain-root
  bridge pattern — no X dep, `Environment=HOME=/root`,
  `Restart=always`, `RestartSec=1`, `ExecStart=/usr/local/bin/hat-sound
  -i` (v3.5 idle; operator's option (c): silent boot, pin LOW).
- All `for unit in ...` loops (converge, repair ×2, verify) extended
  with `sound`; plus a `unit:sound` verify row.
- New `build_sound()`: plan mode → `sound-src` item (src
  exists/absent); apply → missing src: `RESULT: INCOMPLETE:sound-src`
  + exit 1; `gcc -O2 -Wall -Wextra` to an mktemp; build fail →
  `INCOMPLETE:sound-build` exit 1; `cmp -s` against installed →
  identical = no install (zero writes); else `install -m 755` +
  `CHANGED_FILES`. A changed binary marks the sound unit dirty ("a
  binary change is a unit change in disguise") → restart onto the new
  binary.
- Stale overlays removed in apply mode with a logged why
  ("es8388/audio = wrong I2S audio path; HAT amp is GPIO/PWM") —
  `rm -f` + `CHANGED_FILES`, both `/boot/overlay-user/` and
  `/boot/armbian-overlays/`. NOT in `overlay_sig` → their removal is
  not a reboot trigger.
- New verify rows: `rt-sysctl` (PASS iff
  `/proc/sys/kernel/sched_rt_runtime_us` == `-1` — sysctl binary
  ABSENT on the board; the drop-in rides on static
  `systemd-sysctl.service`), `audio-cards` (PASS iff
  `/proc/asound/cards` has EXACTLY one card, `allwinnerhdmi` — an
  es8388 card would mean the wrong path came back), `stale-overlays`
  (PASS iff no es8388-audio/i2c0 dtbo in either overlay dir AND no
  `/sys/bus/i2c/devices/i2c-0`).

MUST-DO in the SAME change (AGENTS.md mandate) — STATUS:
1. **DONE** — `docs/setup.md` rows: five-unit row (incl.
   `gamepi-sound`), `hat-sound` build/install row, sysctl drop-in row
   (with the why), `user_overlays` set-convergence row, stale-overlay
   removal row (with the verified-2026-09-15 rationale), new verify
   rows 9/10/11, preflight `gcc` + `tools/hat-sound.c`, new exit codes
   `sound-src`/`sound-build`, reboot-policy + new **"## Sound
   (hat-sound, v3.5)"** section (service semantics + 3-step manual play
   procedure).
2. **DONE** — `/etc/sysctl.d/99-sched-rt.conf` (644 root) persists
   `-1`; `systemd-sysctl.service` (static) applies it at every boot,
   so a pre-fold-in reboot no longer regresses the 1 Hz × ~50 ms
   RT-bandwidth freeze (sysctl binary absent on the board — that is why
   the drop-in exists).
3. **Board side DONE, $HOME OUT OF SCOPE** — the apply removes
   `es8388-audio.dtbo` + `i2c0.dtbo` from
   `/boot/overlay-user/` and `/boot/armbian-overlays/` with a logged
   why; the `$HOME` copies (`/home/cj/es8388-audio.{dtbo,dts}`,
   `/home/cj/i2c0.dtbo`) are deliberately NOT touched by `setup.sh` —
   operator `rm` when convenient (housekeeping below).
4. **DONE** — verify rows `rt-sysctl`, `audio-cards`,
   `stale-overlays`, `unit:sound` (all implemented; `/dev/input/eventN`
   for buttons stays a future row).

BEFORE commit (live board, operator runs) — PENDING, this is the gate
on the commit:
- Boot-behavior decision: SETTLED by operator → option **(c)** silent
  idle (pin LOW, no sound at boot); implemented as v3.5 `-i`.
- `bash -n setup.sh` — DONE (pass).
- `sudo bash setup.sh --plan` drift audit → operator; expected drift:
  `gamepi-sound.service` (absent), `99-sched-rt.conf` (absent),
  `stale-overlay:es8388-audio` + `stale-overlay:i2c0`; `sound-src` OK;
  NO `user_overlays` drift (board set already equals the desired set).
- `sudo bash setup.sh` (first run) → build+install `/usr/local/bin/
  hat-sound` (absent → install), write drop-in + unit, `rm` both stale
  dtbos (logged), `enable --now gamepi-sound` (the service journal
  should show "hat-sound v3.5: line 37 — idle: pin LOW until stopped"
  = the v3.5 sanity check merged into the apply), NO reboot (overlay
  sig unchanged), verify rows `unit:sound`/`rt-sysctl`/`audio-cards`/
  `stale-overlays` PASS, `RESULT: DRIFT:<n>` exit 0. Second run must be
  `RESULT: CONVERGED` exit 0, zero writes. **The apply→verify on a live
  board is mandatory before commit.**

AFTER (post operator confirmation):
- Commit as `git -c user.name="CJ Trowbridge" -c user.email=
  "chris.j.trowbridge@gmail.com" commit …` (NEVER push).
- `python3 agentic-pipelines/scripts/regenerate_plan_indexes.py
  --repo-root . --check`.
- Move this plan `plans/current/ → plans/past/` when the fold-in is
  committed + verified, then re-run the index check.

## Buttons (user-driven, independent track)

`sudo python3 /home/cj/button-map.py` (OPERATOR runs) to map the 13
membrane buttons → then a `gpio-keys` overlay (pinctrl phandles
2000000.pinctrl banks PB=1/PD=3/PE=4 + 7025000.r-pinctrl PL2;
active-low with pull-up; `linux,code KEY_*`). This resolves the existing
{5,10} vs {11} pin-set ambiguity and folds into the same `user_overlays`
set. Not yet started.

## Housekeeping (operator, when convenient)

- `rm /home/cj/hat-sound-fresh` (no sudo — throwaway v3.3 verification
  rebuild; gate closed)
- `rm /home/cj/es8388-audio.dtbo /home/cj/es8388-audio.dts /home/cj/i2c0
  .dtbo` ($HOME copies — the board-side removal is in setup.sh)
- `sudo rm /home/cj/irq-a /home/cj/irq-b /home/cj/irq-c /home/cj/irq-d`
- optional `sudo rm /home/cj/trace-hat.txt` (15.4 MB) + trace *.py
  parsers after the fold-in
- battery README note; VNC hardening decision (low priority)
- `sched_rt_runtime_us=-1` is now persisted by the sysctl.d drop-in
  (applies at every boot) — the pre-fold-in regression risk is GONE
  once the apply lands; if the board reboots before the apply, the
  freeze still regresses (mention once).

## Post-apply note (2026-09-15 ~13:00 — live apply GREEN, fold-in committed)

Operator ran `sudo bash setup.sh` — first apply: removed
`es8388-audio.dtbo` + `i2c0.dtbo` (logged why), installed the v3.5 build
from tracked source (`/usr/local/bin/hat-sound`), wrote
`/etc/sysctl.d/99-sched-rt.conf` + `gamepi-sound.service`
(`enable --now`; `unit:sound` PASS in-run = v3.5 sanity), NO reboot
(overlay stamp unchanged). Next run: **zero writes, all 13 verify rows
PASS, `RESULT: CONVERGED`, exit 0** — `audio-cards` = `single card
allwinnerhdmi`, `stale-overlays` = clean, `rt-sysctl` = -1.

Two verify bugs found in the fold-in by that first live run, fixed in the
same change (`docs/setup.md` implementation notes, same change):

1. `audio-cards`: awk `-F'[][]'` printed field **$3** (the driver string
   `: allwinner-hdmi - allwinner-hdmi`) instead of the card name in
   **$2** — a healthy single-card board reported `INCOMPLETE:verify`.
   Now: name from $2 + `grep -c '^[[:space:]]*[0-9]'` count enforcing
   "exactly one card" (docs row 10 already said that — the code now
   matches the documented contract).
2. stale-dtbo detection (plan audit + verify row 11) used
   `ls a b 2>/dev/null | grep -q .` under `set -o pipefail`: GNU `ls`
   exits 2 when EITHER operand is missing, so the pipeline always failed
   → the `--plan` audit showed `stale-overlays PASS` + no
   `stale-overlay:*` DRIFT items while the dtbos were still on disk
   (the apply `[[ -f ]]` removal loop had no pipeline and fired anyway —
   the live asymmetry). Now: pure `[[ -f ]]` tests at both sites. Both
   failure modes reproduced + fixed locally before the green re-run.

Plan `2026-09-15-10-01-56_hat-sound-setup-fold-in` closed: validate [x],
commit [x], indexes regenerated, plan `current → past`.

## Do NOT (durable constraints)

- Do NOT repeat "last test / final test" phrasing (operator called it
  out).
- Do NOT hand the operator a stale `/home/cj/hat-sound`; ALWAYS rebuild
  + `ls -la` timestamp-check + (ideally) md5 first.
- Do NOT run root `setup.sh` before the fold-in is written (its
  `apply_overlay()` seds `user_overlays` to a single value).
- Do NOT include `/usr/include/linux/gpio.h` in hat-sound (host uapi ≠
  board kernel uapi); the on-board ABI is inline + static-asserted.
- Do NOT stage anything in `/tmp`; stage in `$HOME` or the repo.
- Do NOT write `/root` (operator owns root; agent stages in $HOME/repo).
- Do NOT retune the tick path / clock (FROZEN, proven).
- Do NOT build audio features beyond ONE feeder/underrun tweak.
- Do NOT push; do NOT delete `/home/cj/hat-sound-v2.c`; do NOT
  disassemble; do NOT ask multi-part diagnostic questions (ask one
  thing); do NOT poll terminals.