# Local working notes (chat transcript; not for the repository)
docs/xfce-freeze-diagnosis.md

# External, board-clone dependencies (cloned at bring-up, never tracked —
# projects/voice-agent/docs/design.md §9; ebe-boilerplate is "your
# boilerplate", whisper.cpp is the pinned submodule third_party/whisper.cpp)
/ebe-boilerplate/

# Local pipeline configuration and runtime evidence (host)
api.yaml
state/
runs/
threads/
artifacts/
failures/
reports/
*.rejected.md
*.rejected.json
*.rejected.txt
*.rejected.*

# Dependency target for the pipeline bootstrap helper
.agentic-pipelines/

# Python tooling
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# Built ALSA plugin artifact — keep the SOURCE (tools/hat_alsa_plugin.c),
# not compiled .so files (build 2026-09-16: build-3 md5 33037e8e… is the
# incident artifact — must not be installed; reinstall it by hand at 3.1r.3)
libasound_module_pcm_hat.so